# 🚀 LemoTunnel Manager

یک اسکریپت مدیریت حرفه‌ای برای راه‌اندازی و مانیتورینگ تونل WebSocket با استفاده از wstunnel و systemd.

---

## ✨ معرفی

LemoTunnel Manager یک ابزار تعاملی (Interactive CLI) برای نصب، پیکربندی و مدیریت wstunnel در دو نقش مختلف است:

* 🌍 سرور خارج (Remote Server)
* 🇮🇷 سرور ایران (Client)

این ابزار به شما کمک می‌کند به‌سادگی یک تونل پایدار برای انتقال ترافیک TCP و UDP (مثلاً WireGuard) روی بستر WebSocket ایجاد کنید.

---

## 🧠 معماری کلی

```
[ Client - Iran ]  ⇄  WebSocket  ⇄  [ Server - Outside ]
      │                                      │
  Local Port                            Local Destination
 (مثلاً 51820)                           (مثلاً 51820)
```

* ارتباط بین دو سرور از طریق WebSocket برقرار می‌شود.
* روی سرور خارج، wstunnel در حالت server اجرا می‌شود.
* روی سرور ایران، wstunnel در حالت client اجرا می‌شود.
* از systemd برای اجرای دائمی و پایدار استفاده شده است.

---

## 📦 پیش‌نیازها

* سیستم‌عامل: Ubuntu / Debian
* دسترسی root
* دسترسی اینترنت برای دریافت آخرین نسخه wstunnel

نکته: اسکریپت به‌صورت خودکار ابزارهای زیر را نصب می‌کند:

* curl
* wget
* jq
* gzip
* unzip
* netcat-openbsd
* psmisc

---

## ⚙️ نصب و اجرا

برای استفاده از LemoTunnel دو روش دارید:

---

### 🚀 روش پیشنهادی (Recommended - نصب تک‌خطی)

سریع‌ترین و ساده‌ترین روش اجرا:

```
bash <(curl -fsSL https://raw.githubusercontent.com/radiolemonatic/lemotunnel-manager/refs/heads/main/lemotunnel.sh)
```

این دستور:

* آخرین نسخه اسکریپت را مستقیم از GitHub دریافت می‌کند
* بدون نیاز به کلون کردن ریپو اجرا می‌شود
* مناسب برای دیپلوی سریع روی سرور تازه

📌 پیشنهاد می‌شود قبل از اجرا، اسکریپت را بررسی کنید تا از صحت آن مطمئن شوید.

---

### 🛠️ روش دستی (Clone از Git)

اگر می‌خواهید سورس را نگه دارید یا ویرایش کنید:

```
git clone https://github.com/radiolemonatic/lemotunnel-manager.git
cd lemotunnel-manager
chmod +x lemotunnel.sh
sudo ./lemotunnel.sh
```

این روش برای توسعه‌دهنده‌ها یا کسانی که می‌خواهند تغییرات شخصی اعمال کنند مناسب‌تر است.

---

## 🛠️ گزینه‌های منو

### 1️⃣ Install Requirements & WSTunnel

* نصب وابستگی‌ها
* دریافت آخرین نسخه wstunnel از GitHub
* نصب در مسیر:

```
/usr/local/bin/wstunnel
```

---

### 2️⃣ Setup Remote Server (Outside)

این گزینه را روی سرور خارج اجرا کنید.

از شما پرسیده می‌شود:

* WebSocket Port (مثلاً 8080)
* Destination Port (مثلاً 51820 برای WireGuard)

سپس سرویس زیر ساخته می‌شود:

```
/etc/systemd/system/wstunnel-server.service
```

و به‌صورت خودکار فعال و اجرا می‌شود.

---

### 3️⃣ Setup Iran Server (Client)

این گزینه را روی سرور ایران اجرا کنید.

از شما پرسیده می‌شود:

* IP سرور خارج
* WebSocket Port
* Local Bind Port
* Destination Port روی سرور خارج
* Protocol (TCP - UDP - Both)

سپس سرویس زیر ساخته می‌شود:

```
/etc/systemd/system/wstunnel-client.service
```

این حالت از TCP و UDP به‌صورت همزمان پشتیبانی می‌کند.

---

### 4️⃣ 📡 Live Connection Test

دو حالت دارد:

* Server Mode → گوش دادن به پکت‌ها
* Client Mode → ارسال پکت تست

برای بررسی سلامت تونل بدون نیاز به ابزار اضافی.

---

### 5️⃣ ⚙️ Manage Tunnels

* ریستارت سرور
* ریستارت کلاینت
* توقف کامل همه سرویس‌ها

---

### 6️⃣ 📝 View Logs

مشاهده 50 لاگ آخر از طریق journalctl برای:

* wstunnel-server
* wstunnel-client

---

### 7️⃣ 🗑️ Uninstall Completely

* حذف سرویس‌ها
* حذف باینری
* پاک‌سازی کامل systemd

---

## 🔐 نمونه سناریو برای WireGuard

روی سرور خارج:

* WireGuard روی پورت 51820
* wstunnel server روی پورت 8080

روی سرور ایران:

* Bind روی 51820
* اتصال به ws://REMOTE_IP:8080

در تنظیمات WireGuard کلاینت، آی‌پی سرور ایران را قرار دهید.

---

## 📊 بررسی وضعیت سیستم

در بالای منو همیشه وضعیت نمایش داده می‌شود:

* نصب بودن هسته
* وضعیت نود ایران
* وضعیت نود خارج
* بررسی Reachability پورت ریموت

---

## 🧩 نکات مهم

* پورت WebSocket باید روی سرور خارج در فایروال باز باشد.
* اگر از Cloudflare استفاده می‌کنید، پورت باید در لیست پورت‌های مجاز باشد.
* برای امنیت بیشتر می‌توانید از reverse proxy و TLS استفاده کنید.

---

## 🧪 تست دستی اتصال

روی سرور ایران:

```
nc 127.0.0.1 PORT
```

روی سرور خارج:

```
nc -l -p DEST_PORT -s 127.0.0.1
```

اگر پیام دریافت شد، تونل سالم است.

---

## 📁 مسیر فایل‌ها

```
/usr/local/bin/wstunnel
/etc/systemd/system/wstunnel-server.service
/etc/systemd/system/wstunnel-client.service
```

---

## ❤️ حمایت از پروژه

اگر این پروژه برای شما مفید بوده و دوست دارید از توسعه آن حمایت کنید، می‌توانید از طریق کریپتو کمک مالی انجام دهید.

### 💰 حمایت با کریپتو (NowPayments)


<a href="https://nowpayments.io/donation/lemotunnel" target="_blank" rel="noreferrer noopener">
  <img src="https://camo.githubusercontent.com/45881539210e2d27d0485c21266cde70c16e0a7770d1f64b05a559a25c25ddee/68747470733a2f2f6e6f777061796d656e74732e696f2f696d616765732f656d626564732f646f6e6174696f6e2d627574746f6e2d626c61636b2e737667" alt="Donate with Crypto via NOWPayments">
</a>


---

## ❤️ توسعه‌دهنده

ساخته‌شده برای مدیریت سریع، تمیز و حرفه‌ای تونل‌های WebSocket.

---

## 📜 لایسنس

استفاده آزاد.

---

✨ LemoTunnel — ساده، تمیز، پایدار.
